export const DefaultInput = (props) => (
  <input
    {...props}
    type="text"
    className="px-2 w-full h-10 italic bg-white py border border-grey rounded-lg focus:outline-none focus:border-blue focus:border-2 max-w-fit"
  />
);
